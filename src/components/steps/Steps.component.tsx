import React from 'react';
import * as Styled from './steps.styles';
import Step from './Step/Step.component';

type OwnProps = {
}

const Steps = (props: OwnProps) => (
  <Styled.StepsWrapper>
    {/* <Step
      title={}
      /> */}
  </Styled.StepsWrapper>
);

export default Steps;